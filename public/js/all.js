new Vue({

    /**
     * Register page is the target.
     */
    el: '#register',

    methods: {
        register: function() {

            // Build post data
            var data = {
                name: this.$get('name')
            };

            // Make request

            // Parse errors to highlight each input

        }
    }
});
//# sourceMappingURL=all.js.map